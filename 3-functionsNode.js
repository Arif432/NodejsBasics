const best = (args) =>{
    console.log(`best man in the Universe is ${args}`);
}

const common = (para) =>{
    console.log( `${para } is a common man ` );
}

module.exports = { best , common }